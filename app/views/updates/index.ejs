<% include ../header %>

<h1><%- title %></h1>

<% if (updates.length) { %>
	<ul>
		<% updates.forEach(function (update) { %>
			<li>
				<%= formatDate(update.dateCreated) %>:
				<strong><%= update.title %>.</strong>
				<%= update.description %>
				<a href="<%= update.url %>" target="_blank">Link</a>
				<a href="<%= update.imageUrl %>" target="_blank">Image</a>
			</li>
		<% }) %>
	</ul>
<% } %>

<% include ../footer %>